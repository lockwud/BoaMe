{
  "name": "boame",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "dev": "pnpm run dev:clean && turbo dev --filter=@boame/web",
    "dev:clean": "node scripts/clean-dev-ports.mjs",
    "dev:all": "turbo dev",
    "dev:mobile": "node scripts/assert-supported-node.mjs && node scripts/clean-mobile-dev.mjs && pnpm --filter @boame/mobile run dev:lan:clear",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "prettier --write ."
  },
  "engines": {
    "node": ">=20 <25"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
