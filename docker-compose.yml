services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: boame
      POSTGRES_PASSWORD: boame
      POSTGRES_DB: boame
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:
